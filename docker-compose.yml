services:
  web:
    build: .
    image: juya-daily-scheduler
    container_name: juya-daily
    restart: always
    ports:
      - "8001:8001"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
